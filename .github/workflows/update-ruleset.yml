name: Update Rulesets

on:
  schedule:
    - cron: '0 5 * * *'
  push:
    branches:
      - sing-box
  workflow_dispatch:

jobs:
  update:
    if: github.ref == 'refs/heads/sing-box'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
        
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.13'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas requests pyyaml
      
      - name: Generate rulesets
        run: python main.py
      
      - name: Git push srs
        run: |
          cd output/srs || exit 1
          git init -b srs_release
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "update srs"
          git push -f "https://${{ github.actor }}:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}" srs_release

      - name: Git push json
        run: |
          cd output/json || exit 1
          git init -b json_release
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "update json"
          git push -f "https://${{ github.actor }}:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}" json_release
